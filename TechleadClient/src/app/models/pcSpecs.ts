/**
 * PcSpecs - interface for a Specs entity
 */
export interface PcSpecs {
  cpu: string;
  gpu: string;
  motherBoard: string;
  ram: string;
  storage: string;
  storageUnit: string;
  case: string;
  powerSupplyUnit: string;
  cooling: string;
  monitor: string;
  pump: string;
  tubes: string;
  coolingLiquid: string;
  cables: string;
}
